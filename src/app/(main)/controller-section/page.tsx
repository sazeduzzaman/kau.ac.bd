import SectionController from "@/components/SectionController/SectionController";

const page = () => {
    return (
        <div>
            <SectionController />
        </div>
    );
};

export default page;